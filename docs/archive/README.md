# Archive

Historical, internal working artifacts preserved for context. These are not
maintained going forward — see the top-level `docs/` for the current
architecture, roadmap, parity, and project brief.

- `parity-foundation-plan.md` — the original contract-first foundation plan.
- `session-handoff-template.md` — template used for cross-session handoffs.
- `handoffs/` — point-in-time session handoff notes from the early build.
