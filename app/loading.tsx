export default function Loading() {
  return (
    <main className="page-wrap flex min-h-[60vh] items-center justify-center">
      <p className="font-mono text-sm text-[var(--ink-faint)]">Loading...</p>
    </main>
  );
}
